# PrometheusGPT Mini - Docker Compose
# Author: MagistrTheOne, Krasnodar, 2025

version: '3.8'

services:
  prometheusgpt:
    build: .
    container_name: prometheusgpt-mini
    ports:
      - "8000:8000"
    volumes:
      - ./data:/home/prometheusgpt/app/data
      - ./models:/home/prometheusgpt/app/models
      - ./logs:/home/prometheusgpt/app/logs
    environment:
      - CUDA_VISIBLE_DEVICES=0
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Опциональный мониторинг
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheusgpt-monitor
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
    depends_on:
      - prometheusgpt

volumes:
  prometheus_data:
